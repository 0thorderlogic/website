---
import "../styles/global.css";
interface Props {
  title: string;
  description?: string;
  image?: string;
  ogType?: "website" | "article";
}

import { pageDescription, name, url } from "../../consts";
import Navbar from "../components/ui/Navbar.astro";
import Footer from "../components/ui/Footer.astro";
const pageSlug = Astro.url.pathname.replace(/^\//, "").replace(/\/$/, "") || "index";
const { title, image = `/og/${pageSlug}.png`, ogType = "website" } = Astro.props;
const siteUrl = Astro.site?.toString() ?? url;
const pageTitle = `${name}'s ${title}`;
const canonicalUrl = new URL(Astro.url.pathname, siteUrl).toString();
const previewImageUrl = new URL(image, siteUrl).toString();
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={pageDescription} />
    <link rel="canonical" href={canonicalUrl} />
    <meta property="og:type" content={ogType} />
    <meta property="og:site_name" content={`${name}'s Website`} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={pageDescription} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={previewImageUrl} />
    <meta property="og:image:alt" content={pageTitle} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={pageDescription} />
    <meta name="twitter:image" content={previewImageUrl} />
    <title>{pageTitle}</title>
  </head>
  <body>
    <div class="mx-auto flex min-h-screen w-full max-w-7xl flex-col px-5">
      <header class="pt-6 flex justify-between items-center gap-4">
        <h1 class="title-1 mb-0!">{name}'s {title}</h1>
        <Navbar />
      </header>
      <div class="flex-1 py-6">
        <slot />
      </div>
      <Footer />
    </div>
  </body>
</html>
